# No external dependencies required for the CLI/Bot version.
# Pure Python Standard Library (datetime, hashlib, random)

# For the Web Version (app.py):
Quart>=0.19.0
hypercorn>=0.17.0

# Optional: For production deployment (Linux)
# gunicorn>=20.1.0
